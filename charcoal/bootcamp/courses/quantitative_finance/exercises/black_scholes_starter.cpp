// Black-Scholes pricing starter
// Input: S K r sigma T  (spot, strike, rate, vol, time)
#include <iostream>

int main(){
    double S,K,r,sigma,T;
    if(!(std::cin>>S>>K>>r>>sigma>>T)) return 0;
    // TODO: implement Black-Scholes formula, print option price
    std::cout<<"TODO: compute Black-Scholes price for "<<S<<" "<<K<<"\n";
}
