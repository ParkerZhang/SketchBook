// Exercise 1: Implement factorial iteratively.
// Read an integer n (0 <= n <= 20) and print n!.
#include <iostream>

long long factorial(int n) {
    // TODO: implement
    return -1;
}

int main() {
    int n;
    if (!(std::cin >> n)) return 0;
    std::cout << factorial(n) << "\n";
}
